<Window x:Class="iNeedMyMoneyBack.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:iNeedMyMoneyBack"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="ConfigWindow"
        Width="420"
        Height="450"
        MinWidth="350"
        MinHeight="300"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="{DynamicResource FontConfig}"
        KeyDown="Window_KeyDown"
        OpacityMask="White"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        mc:Ignorable="d">
    <Window.Resources>
        <SolidColorBrush x:Key="TextColor" />
        <SolidColorBrush x:Key="CheckedColor" />
        <SolidColorBrush x:Key="CardBackground" />
        <SolidColorBrush x:Key="CardBorder" />
        <SolidColorBrush x:Key="TabBackground" />
        <SolidColorBrush x:Key="InputBackground" />
        <SolidColorBrush x:Key="ScrollThumb" />
        <SolidColorBrush x:Key="ScrollTrack" />
        <Thickness x:Key="BorderThickness" />
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Padding="6,3"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource CardBorder}"
                                BorderThickness="1"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackground}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="4,3" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="border"
                                Padding="6,4"
                                Background="Transparent"
                                BorderBrush="{DynamicResource CardBorder}"
                                BorderThickness="1"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackground}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource CheckedColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Margin" Value="2" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBackground}" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="border"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5" />
                            <ContentPresenter x:Name="contentPresenter"
                                              Grid.Column="0"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding ItemStringFormat}" />
                            <ToggleButton x:Name="toggleButton"
                                          Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path x:Name="arrow"
                                                  HorizontalAlignment="Right"
                                                  Margin="0,0,6,0"
                                                  VerticalAlignment="Center"
                                                  Data="M0,0 L4,4 L8,0 z"
                                                  Fill="{DynamicResource TextColor}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="arrow" Property="Fill" Value="{DynamicResource CheckedColor}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="popup"
                                   AllowsTransparency="True"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   PopupAnimation="None">
                                <Border x:Name="dropDownBorder"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="{DynamicResource InputBackground}"
                                        BorderBrush="{DynamicResource CardBorder}"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        Opacity="{DynamicResource MainOpacity}">
                                    <ScrollViewer>
                                        <VirtualizingStackPanel IsItemsHost="True" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackground}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackground}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextColor}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border"
                                Padding="8,4"
                                Background="Transparent"
                                BorderBrush="{DynamicResource CardBorder}"
                                BorderThickness="0,0,0,2">
                            <ContentPresenter x:Name="content"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource HoverBackground}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" />
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Grid.Row="0"
                                       Width="8"
                                       Background="{DynamicResource ScrollTrack}"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Column="0"
                                       Grid.Row="1"
                                       Height="8"
                                       Background="{DynamicResource ScrollTrack}"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{DynamicResource ScrollThumb}"
                                                        CornerRadius="4"
                                                        Margin="1" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="grid" Background="Transparent">
        <Border Name="border"
                BorderBrush="#000"
                BorderThickness="{DynamicResource BorderThickness}"
                CornerRadius="0,10,0,10">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TabControl x:Name="mainTab" Grid.Row="0">
                    <!-- 股票管理 Tab -->
                    <TabItem Name="tab_stocks" Header="股票管理">
                        <Grid Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <!-- 添加股票区域 -->
                            <Border Grid.Row="0"
                                    Margin="0,0,0,3"
                                    Padding="0"
                                    BorderThickness="0"
                                    CornerRadius="0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="txtStockCode"
                                             Grid.Column="0"
                                             VerticalContentAlignment="Center"
                                             Padding="3"
                                             KeyDown="TxtStockCode_KeyDown">
                                        <TextBox.Template>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="0"
                                                        CornerRadius="5"
                                                        Padding="{TemplateBinding Padding}"
                                                        SnapsToDevicePixels="True">
                                                    <ScrollViewer x:Name="PART_ContentHost" />
                                                </Border>
                                            </ControlTemplate>
                                        </TextBox.Template>
                                    </TextBox>
                                    <Button x:Name="btn_add_stock"
                                            Grid.Column="1"
                                            Margin="2,0,0,0"
                                            Click="Btn_Add_Stock_Click" />
                                </Grid>
                            </Border>
                            <!-- 股票列表 -->
                            <ScrollViewer x:Name="stockListScroll"
                                          Grid.Row="1"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="stockListPanel" />
                            </ScrollViewer>
                        </Grid>
                    </TabItem>
                    <!-- 显示字段 Tab -->
                    <TabItem Name="tab_fields" Header="显示字段">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="5">
                                <!-- 基础字段 -->
                                <TextBlock x:Name="lbl_basic_fields"
                                           Margin="0,0,0,5"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource TextColor}" />
                                <WrapPanel x:Name="FieldControls" Orientation="Horizontal" />
                                <!-- 指数监控 -->
                                <Separator Margin="0,10" Background="{DynamicResource CardBorder}" />
                                <TextBlock x:Name="lbl_index_fields"
                                            Margin="0,0,0,5"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource TextColor}" />
                                <TextBlock x:Name="lbl_index_a"
                                            Margin="4,8,4,2"
                                            FontSize="11"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource TextColor}"
                                            Opacity="0.6" />
                                <WrapPanel x:Name="StockIndexControlsA" Orientation="Horizontal" />
                                <TextBlock x:Name="lbl_index_hk"
                                            Margin="4,8,4,2"
                                            FontSize="11"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource TextColor}"
                                            Opacity="0.6" />
                                <WrapPanel x:Name="StockIndexControlsHK" Orientation="Horizontal" />
                                <TextBlock x:Name="lbl_index_us"
                                            Margin="4,8,4,2"
                                            FontSize="11"
                                            FontWeight="Bold"
                                            Foreground="{DynamicResource TextColor}"
                                            Opacity="0.6" />
                                <WrapPanel x:Name="StockIndexControlsUS" Orientation="Horizontal" />
                                <!-- 扩展字段 -->
                                <Separator Margin="0,10" Background="{DynamicResource CardBorder}" />
                                <TextBlock x:Name="lbl_extend_fields"
                                           Margin="0,0,0,5"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource TextColor}" />
                                <WrapPanel x:Name="ExtendControls" Orientation="Horizontal" />
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                    <!-- 更多设置 Tab -->
                    <TabItem Name="tab_more_settings" Header="更多设置">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,5,5,0">
                                <Grid Grid.IsSharedSizeScope="True">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <!-- 第一行：主窗口字体大小 + 列间距设置 -->
                                    <TextBlock x:Name="lbl_font_size"
                                               Grid.Row="0"
                                               Grid.Column="0"
                                               Margin="4"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right"
                                               Foreground="{DynamicResource TextColor}" />
                                    <ComboBox x:Name="cmbFontSize"
                                              Grid.Row="0"
                                              Grid.Column="1"
                                              Margin="2"
                                              Padding="4"
                                              MinHeight="25"
                                              SelectionChanged="CmbFontSize_SelectionChanged" />
                                    <TextBlock x:Name="lbl_column_spacing"
                                               Grid.Row="0"
                                               Grid.Column="2"
                                               Margin="4"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right"
                                               Foreground="{DynamicResource TextColor}" />
                                    <ComboBox x:Name="cmbColumnSpacing"
                                              Grid.Row="0"
                                              Grid.Column="3"
                                              Margin="2"
                                              Padding="4"
                                              MinHeight="25"
                                              SelectionChanged="CmbColumnSpacing_SelectionChanged" />
                                    <!-- 第二行：数据查询间隔 + 双击跳转设置 -->
                                    <TextBlock x:Name="lbl_interval"
                                               Grid.Row="1"
                                               Grid.Column="0"
                                               Margin="4"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right"
                                               Foreground="{DynamicResource TextColor}" />
                                    <ComboBox x:Name="cmbInterval"
                                              Grid.Row="1"
                                              Grid.Column="1"
                                              Margin="2"
                                              Padding="4"
                                              MinHeight="25"
                                              SelectionChanged="CmbInterval_SelectionChanged" />
                                    <TextBlock x:Name="lbl_double_click_action"
                                               Grid.Row="1"
                                               Grid.Column="2"
                                               Margin="4"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right"
                                               Foreground="{DynamicResource TextColor}" />
                                    <ComboBox x:Name="cmbDoubleClickAction"
                                              Grid.Row="1"
                                              Grid.Column="3"
                                              Margin="2"
                                              Padding="4"
                                              MinHeight="25"
                                              SelectionChanged="CmbDoubleClickAction_SelectionChanged" />
                                    <!-- 第三行：API设置（独占整行） -->
                                    <TextBlock x:Name="lbl_api"
                                               Grid.Row="2"
                                               Grid.Column="0"
                                               Margin="4"
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right"
                                               Foreground="{DynamicResource TextColor}" />
                                    <Grid Grid.Row="2"
                                          Grid.Column="1"
                                          Grid.ColumnSpan="3">
                                        <TextBox x:Name="txtApi"
                                                 VerticalContentAlignment="Center"
                                                 MinHeight="25"
                                                 Padding="4"
                                                 TextChanged="TxtApi_TextChanged">
                                            <TextBox.Template>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="0"
                                                            CornerRadius="5"
                                                            Padding="{TemplateBinding Padding}"
                                                            SnapsToDevicePixels="True">
                                                        <ScrollViewer x:Name="PART_ContentHost" />
                                                    </Border>
                                                </ControlTemplate>
                                            </TextBox.Template>
                                        </TextBox>
                                        <TextBlock x:Name="txtApiApply"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   FontWeight="Bold"
                                                   Cursor="Hand"
                                                   Visibility="Collapsed"
                                                   MouseLeftButtonDown="TxtApiApply_Click" />
                                    </Grid>
                                </Grid>
                                <!-- 按钮区域 -->
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Right"
                                            Margin="0,10,0,0">
                                    <Button x:Name="btn_reset_api"
                                            Click="Btn_Reset_Api_Click"
                                            Content="{Binding Text, ElementName=txtResetLabel}" />
                                    <TextBlock x:Name="txtResetLabel" Visibility="Collapsed" />
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
                <!-- 底部按钮 -->
                <Grid Grid.Row="1" Margin="0,5,0,0">
                    <Button x:Name="btn_close"
                            HorizontalAlignment="Left"
                            Click="Btn_Close_Click" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
